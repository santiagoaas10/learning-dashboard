# Marca la carpeta "routers" como paquete de Python.
# Aquí agrupamos las rutas por tema (un archivo por recurso), en vez de meter
# todos los endpoints en main.py. Escala mucho mejor a medida que crece la API.
